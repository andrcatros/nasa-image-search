import React from "react";

import "../styles/ImageResults.css";

const ImageResults = ({ searchResults }) => {
  return <div className="image=results" data-testid="image-results-test"></div>;
};

export default ImageResults;
